# Install as /etc/default/mqtt_reporterd. The startup script loads this file.
MQTT_DEVICE_ID=rk3568-001
MQTT_BROKER_HOST=192.168.5.2
MQTT_BROKER_PORT=1883

# 0: first start reports only new events; 1: first start replays all history.
MQTT_REPLAY_HISTORY=0
